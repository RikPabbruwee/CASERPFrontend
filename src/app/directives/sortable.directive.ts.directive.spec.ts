import { SortableDirectiveTsDirective } from './sortable.directive.ts.directive';

describe('SortableDirectiveTsDirective', () => {
  it('should create an instance', () => {
    const directive = new SortableDirectiveTsDirective();
    expect(directive).toBeTruthy();
  });
});
